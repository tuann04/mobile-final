import 'package:flutter/material.dart';

class TanScreen extends StatelessWidget {
  const TanScreen({required Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tan Screen'),
      ),
      body: Center(
        child: Text('Welcome dasfsd to Tan Screen'),
      ),
    );
  }
}
